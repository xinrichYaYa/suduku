{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Neutralino",
            "type": "shell",
            "command": "neu run",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": []
        }
    ]
}
